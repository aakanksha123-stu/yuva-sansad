services:
  - type: web
    name: yuva-sansad-backend
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn Backend.wsgi:application
    rootDirectory: backend  # where manage.py livesservices:
  - type: web
    name: yuva-sansad-backend
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn Backend.wsgi:application
    rootDirectory: backend  # where manage.py lives